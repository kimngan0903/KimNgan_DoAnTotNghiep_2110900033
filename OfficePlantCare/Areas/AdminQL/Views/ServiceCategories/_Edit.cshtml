@model OfficePlantCare.Models.ServiceCategory

@{
    ViewData["Title"] = "Chỉnh sửa Danh mục dịch vụ";
}

<link rel="stylesheet" href="~/assets/css/edit.css" />

<div class="card shadow-sm border-0 rounded-lg p-1">
    <h3 class="text-success text-center font-weight-bold mb-4"><i class="fa-solid fa-pen-to-square me-2"></i>CHỈNH SỬA DANH MỤC DỊCH VỤ</h3>
    <form asp-action="Edit" method="post">
        <input type="hidden" asp-for="CategoryServiceId" />

        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="form-group mb-4">
                    <i class="fa-solid fa-list me-2"></i>
                    <label asp-for="CategoryServiceName" class="control-label fw-bold text-muted"></label>
                    <input asp-for="CategoryServiceName" class="form-control rounded-pill" placeholder="Nhập tên danh mục dịch vụ" />
                    <span asp-validation-for="CategoryServiceName" class="text-danger"></span>
                </div>
            </div>
        </div>

        <!-- Buttons -->
        <div class="form-group d-flex gap-3 justify-content-center">
            <button type="submit" class="btn btn-primary rounded-pill px-4"><i class="fas fa-save me-2"></i>Lưu</button>
            <a asp-action="Index" class="btn btn-secondary rounded-pill px-4"><i class="fas fa-arrow-left me-2"></i>Quay lại</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}